[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "LimbleConnector"
version = "0.1.0"
description = ""
#authors = [
#    { name = "Your Name", email = "you@example.com" }
#]
#readme = "README.md"
#requires-python = ">=3.7"
#license = { text = "MIT" }
dependencies = [
    "cachetools>=5.5.2",
    "openpyxl>=3.1.5",
    "pandas>=2.2.3",
    "pulp>=3.1.1",
    "requests>=2.32.3",
    "tzlocal>=5.3.1",
]


[tool.setuptools]
packages = ["LimbleConnection"]
